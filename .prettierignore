node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml

# apps/web — ยกมาจาก MSU Starter Kit · ไฟล์ที่ generate หรือเป็นของเทมเพลตล้วน
# (โค้ดใน apps/web/src ยัง format ตามปกติ โดย prettier ใช้ apps/web/.prettierrc.json
#  ซึ่งเป็นสไตล์ของเทมเพลต — ไม่มี semicolon · printWidth 120)
apps/web/.next/
# next dev/build เขียนทับไฟล์นี้เองทุกครั้ง (อยู่ใน .gitignore อยู่แล้ว)
apps/web/next-env.d.ts
apps/web/public/
apps/web/src/assets/iconify-icons/generated-icons.*
apps/api/dist/

# ไฟล์อ้างอิงจาก prototype เดิม — ห้าม format (ต้องคงต้นฉบับไว้เทียบสูตร)
MSU-BEPS_*.html
MSU-BEPS*.html
จุดคุ้มทุน_*.html
WIREFRAME.html

# เอกสารที่ generate ขึ้น — ER.html ฝังไลบรารี Mermaid ไว้ 3.4 MB ห้าม format
ER.html

# เอกสารจากภายนอก — ห้าม format เพื่อคงต้นฉบับไว้เทียบ
MANUS/

# mockup — เอกสารนำเสนอ ห้าม format
MOCKUP.html
MOCKUP-MASTER.html

# mockup/ — หน้าจอ W0–W17 · markup และสูตรยกมาจาก prototype v8-1 ต้องคงต้นฉบับไว้เทียบ
# (mockup/README.md ไม่อยู่ในข้อยกเว้น — format ตามปกติ)
mockup/*.html
mockup/assets/
apps/web/src/data/fixtures/raw.data.json
