# MSU-BEPS — Mockup หน้าจอ W0–W19

Mockup ครบทุกหน้าจอตาม [`SA.md`](../SA.md) §9 **บวกหน้าจัดการข้อมูลที่ SA ยังไม่ได้ระบุไว้ (W16–W19)**
· หนึ่งหน้าจอ = หนึ่งไฟล์ HTML
ใช้สไตล์ สูตรคำนวณ และข้อมูลจริงปีงบประมาณ 2568 ชุดเดียวกับ prototype `v8-1`

## วิธีเปิด

เปิดไฟล์ตรงจากเบราว์เซอร์ได้เลย ไม่ต้อง build — เริ่มที่ [`index.html`](index.html)

หรือเสิร์ฟผ่าน HTTP (เลี่ยงข้อจำกัดบางอย่างของ `file://`):

```bash
npx --yes http-server mockup -p 8765 -c-1 -o
```

## รายการหน้าจอ

| ไฟล์                       | W   | หน้าจอ                                                      | ที่มา           |
| -------------------------- | --- | ----------------------------------------------------------- | --------------- |
| `W0-login.html`            | W0  | เข้าสู่ระบบ + ตารางสิทธิ์ role → สิ่งที่เห็น                | ใหม่            |
| `W1-overview.html`         | W1  | ภาพรวมมหาวิทยาลัย                                           | v8-1            |
| `W2-breakeven.html`        | W2  | เจาะลึกจุดคุ้มทุน 3 ระดับ                                   | v8-1            |
| `W3-chart.html`            | W3  | กราฟจุดคุ้มทุน                                              | v8-1            |
| `W4a-perhead.html`         | W4  | รายได้ vs ต้นทุนต่อหัว                                      | v8-1            |
| `W4b-cost.html`            | W4  | โครงสร้างต้นทุน                                             | v8-1            |
| `W4c-revenue.html`         | W4  | รายได้รายคณะ                                                | v8-1            |
| `W5-cross.html`            | W5  | Cross Analysis & Heatmap                                    | v8-1            |
| `W6-scenario-faculty.html` | W6  | คำนวณจุดคุ้มทุนรายคณะ                                       | v8-1            |
| `W7-scenario-program.html` | W7  | คำนวณจุดคุ้มทุนรายหลักสูตร + รายงาน PDF                     | v8-1            |
| `W8-tuition.html`          | W8  | ค่าธรรมเนียม + workflow อนุมัติ                             | ใหม่            |
| `W9-cost-data.html`        | W9  | ข้อมูลต้นทุน · สถานะ sync · validation                      | ใหม่            |
| `W10-method.html`          | W10 | สูตร & หลักวิชาการ                                          | v8-1            |
| `W11-allocation-run.html`  | W11 | คอนโซลรอบคำนวณ (Allocation Run)                             | ใหม่            |
| `W12-reconciliation.html`  | W12 | ผลตรวจยอด + direct เทียบ allocated                          | ใหม่            |
| `W13-exceptions.html`      | W13 | รายการค้างตรวจ (Exception queue)                            | ใหม่            |
| `W14-account-rules.html`   | W14 | กติกาผังบัญชี TFC/TVC + ไทม์ไลน์รายปี                       | ใหม่            |
| `W15-settings.html`        | W15 | นโยบายการคำนวณ (ค่าตั้งระบบ)                                | ใหม่            |
| `W16-programs.html`        | W16 | ทะเบียนหลักสูตร — เปิดใหม่ · ปรับปรุงรอบ มคอ. · งดรับ · ปิด | **เพิ่มจาก SA** |
| `W17-master-data.html`     | W17 | ทะเบียนหน่วยงาน · งวดปีงบประมาณ · ประเภทนิสิต               | **เพิ่มจาก SA** |

## โครงสร้างไฟล์

```
mockup/
├── index.html              สารบัญหน้าจอทั้งหมด
├── W*.html                 หน้าจอ 22 ไฟล์
└── assets/
    ├── beps.css            สไตล์ยกมาจาก v8-1 ทั้งชุด (ไม่แก้)
    ├── extra.css           คอมโพเนนต์ของหน้าจอใหม่ + ปรับเปลือกหน้าจอ
    ├── logo.js             ตราสัญลักษณ์ มมส. (base64)
    ├── data.sample.js      ★ ข้อมูล "ตัวอย่าง" (สุ่มรบกวนแล้ว) — ไฟล์เดียวที่อยู่ใน git
    ├── data.js             ข้อมูลจริงปี 2568 — .gitignore กันไว้ ไม่ขึ้น git
    ├── master-data.js      ข้อมูลจำลองของหน้าจัดการ (W8, W9, W11–W15)
    ├── core.js             สูตรคำนวณ + เปลือกหน้าจอ (sidebar / topbar)
    └── page-*.js           ตรรกะเฉพาะหน้า
```

### `core.js`

รวมสูตรทั้งหมดที่ยกมาจาก v8-1 **โดยไม่แก้ค่า** — `R()`, `Qs()`, `TRc()`, `AVC()`, `ATC()`,
`profit()`, `status()` และตัวจัดรูปแบบตัวเลข

`buildShell({ w, title, f, bm, run })` วาด sidebar + topbar ให้ทุกหน้า
หน้าที่ต้องวาดใหม่เมื่อสลับฐานรายได้ ให้ประกาศ `window.renderPage`

`dataCaveat()` คืน HTML แถบเตือนว่าค่าเสื่อมราคาอาคารยังไม่ครบ — ติดไว้ทุกหน้าที่แสดงส่วนเกินหรือ Q\*

### `data.js`

ยกมาจาก v8-1 ทั้งก้อน (snapshot `20260711`)

| คีย์        | จำนวน | คำอธิบาย            |
| ----------- | ----- | ------------------- |
| `RAW.UNI`   | 1     | ยอดรวมมหาวิทยาลัย   |
| `RAW.FACS`  | 20    | รายคณะ / วิทยาลัย   |
| `RAW.DEPTS` | 38    | คณะ × ระดับการศึกษา |
| `RAW.PROGS` | 230   | รายหลักสูตร         |

### `master-data.js`

ข้อมูลจำลองของหน้าจัดการ — โครงสร้างอ้างอิงตารางจริงใน `db/01_schema.sql`

| ค่า                                       | ตารางที่อ้าง                                                | ใช้ที่    |
| ----------------------------------------- | ----------------------------------------------------------- | --------- |
| `FEES` · `FEE_LOG`                        | `fee_schedule` · `fee_approval_log`                         | W8        |
| `SOURCES` · `VALIDATIONS`                 | `import_batch` · `data_quality_issue`                       | W9        |
| `RUNS` · `RUN_LOG`                        | `allocation_run`                                            | W11       |
| `RECON_METHODS`                           | `reconciliation_control` · `allocation_result`              | W12       |
| `EXCEPTIONS`                              | `data_quality_issue`                                        | W12 · W13 |
| `ACCOUNTS`                                | `erp_account` · `account_behavior_rule`                     | W14       |
| `SETTINGS` · `SETTING_LOG`                | `system_setting_def` · `system_setting`                     | W15       |
| `PROGRAM_REG` · `PROGRAM_LOG`             | `program` · `program_version` · `audit_event`               | W16       |
| `ORG_UNITS` · `PERIODS` · `STUDENT_TYPES` | `org_unit` · `org_unit_map` · `dim_period` · `student_type` | W17       |
| `ROLES` · `RBAC`                          | `app_user` · `app_role`                                     | W0        |

## ข้อมูลจริง เทียบ ข้อมูลตัวอย่าง

`.gitignore` ของโปรเจกต์ห้าม commit ข้อมูลการเงินภายในมหาวิทยาลัย และ repo นี้เป็น **public**
`mockup/assets/data.js` (งบ · ต้นทุน · ค่าเสื่อม · นิสิต แยกครบ 20 คณะ 230 หลักสูตร) จึง**ไม่อยู่ใน git**

ทุกหน้าโหลดข้อมูลแบบนี้:

```html
<script src="assets/data.sample.js"></script>
<!-- ถ้ามี data.js ในเครื่อง จะโหลดทับให้เอง -->
<script src="assets/data.js"></script>
```

| สถานการณ์                                | เห็นตัวเลขอะไร                                            |
| ---------------------------------------- | --------------------------------------------------------- |
| clone จาก GitHub มาเปล่าๆ                | ชุดตัวอย่าง + **แถบแดงเตือนบนทุกหน้า**ว่าไม่ใช่ตัวเลขจริง |
| วาง `data.js` ของจริงลง `mockup/assets/` | ตัวเลขจริง แถบแดงหายไปเอง                                 |

ชุดตัวอย่างสร้างจาก `mockup/make-data-sample.mjs` — สุ่มรบกวนตัวตั้งรายหลักสูตร
แล้ว**คำนวณค่าที่เหลือใหม่ตามสูตรเดิมทั้งหมด** ความสัมพันธ์ทุกสูตรจึงยังถูกต้อง
(`TR = st + own` · `TC = TFC + TVC` · `Q* = TFC ÷ (R − AVC)` · สูตรที่ 7 เมื่อ CM ≤ 0)
หน้าจอทุกหน้าจึงทำงานเหมือนใช้ข้อมูลจริง แต่ยอดรวมห่างจากของจริงราว 30%

```bash
node mockup/make-data-sample.mjs   # สร้างใหม่เมื่อ data.js เปลี่ยน
```

## สิ่งที่เปลี่ยนจาก prototype v8-1

หน้า W1–W7 และ W10 ใช้ markup และตรรกะเดิม **ไม่แก้สูตรแม้แต่บรรทัดเดียว** ส่วนที่เพิ่มคือ

1. แยกไฟล์ต่อหน้า และแยก CSS / ข้อมูล / สูตร ออกเป็น `assets/`
2. topbar เพิ่ม **ปีงบประมาณ**, **รอบคำนวณที่กำลังดู** และ **ผู้ใช้ที่เข้าสู่ระบบ** (SA §9.1 ข้อ 2, §9.2 ข้อ 9)
3. เพิ่ม **แถบเตือนข้อจำกัดของข้อมูล** ในทุกหน้าที่แสดงส่วนเกินหรือ Q\*
4. sidebar เพิ่มกลุ่มเมนู ปันส่วนต้นทุน · จัดการข้อมูล · ตั้งค่าระบบ พร้อมไอคอนล็อกบอกว่าเห็นตาม role

## W16–W19 — หน้าที่ SA §9 ยังไม่ได้ระบุ

ตารางหน้าจอใน SA §9 ครอบคลุมเฉพาะ W0–W15 ซึ่ง**ไม่มีหน้าจัดการตัวทะเบียนหลักสูตรเอง**
มีแต่ W7 ที่เป็นเครื่องจำลอง (`scenario_plan`) — สร้าง `program` จริงไม่ได้
ทั้งที่ ER v2 มีตาราง `program` + `program_version` และ SA ระบุเองว่า
`program_version` รองรับการปรับปรุงหลักสูตรรอบ มคอ.

**W16** ปิดช่องว่างนั้น: เปิดหลักสูตรใหม่ · ปรับปรุงรอบ มคอ. (สร้างรุ่นใหม่ ไม่ทับของเดิม)
· งดรับนิสิต · ปิดหลักสูตร + บล็อกตรวจความพร้อมก่อนเปลี่ยนสถานะเป็น "เปิดสอน"

**W17** ครอบคลุมข้อมูลหลักที่เหลือซึ่งยังไม่มีหน้าจอเช่นกัน — `org_unit` / `org_unit_map`
· `dim_period` · `student_type`

**W18** จัดการ `app_user` — W0 แสดงตารางสิทธิ์แต่แก้ไม่ได้

**W19** จัดการ `erp_account` — W14 แก้กติกา TFC/TVC ได้ แต่เพิ่มบัญชีใหม่ไม่ได้

เพิ่มเข้า `SA.md` §9 แล้ว พร้อมหัวข้อ **§9.4** ที่บันทึกว่ายังไม่มีบทบาท `approver` ใน schema

> ⚠️ **ยังไม่ตัดสิน** — `app_role` ล็อกไว้ 4 ค่าและไม่มี `approver`
> mockup จึงให้ `admin` อนุมัติทุกเรื่องไว้ก่อน และติดป้ายเตือนบน W0 กับ W18

## ข้อควรระวัง

- **ค่าเสื่อมราคาอาคารยังไม่มีข้อมูล** — TFC และ TC ต่ำกว่าจริง ส่วนเกิน +32.8 ลบ. สูงเกินจริง
  และ Q\* ทุกระดับต่ำกว่าที่ควรเป็น · ตัวเลขชุดนี้ยังไม่ใช่ตัวเลขสำหรับนำเสนอเป็นข้อสรุป
- `master-data.js` เป็น **ข้อมูลจำลอง** — โครงสร้างอ้างอิงตารางจริงใน `db/01_schema.sql`
  แต่ชื่อผู้ใช้ วันเวลา และสถานะ workflow เป็นตัวอย่างเพื่อให้เห็นรูปแบบหน้าจอ
- W12 ใช้ `tfcOffice` เป็นตัวแทนของยอดที่ปันส่วนในตารางรายคณะ
  ระบบจริงต้องอ่านจาก `cost_allocation_line.method`
- W16 จับคู่กับ `RAW.PROGS` ด้วย (ชื่อหลักสูตร + คณะ + ระดับ) เพราะชุดข้อมูล v8-1 ไม่มี `program_code`
  ระบบจริงต้องจับคู่ด้วย `program_code` จากระบบทะเบียนเท่านั้น
