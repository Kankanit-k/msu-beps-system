# กระบวนการทำงาน — การจัดสรรต้นทุนคงที่ (Fixed Cost Allocation)

> ออกแบบตามมติที่ประชุม: ให้คณะเลือกวิธีจัดสรรต้นทุนคงที่ได้ 3 รูปแบบ
> (Per Head/FTES · Per Program · Custom Percentage)
> เอกสารนี้ต่อยอดจาก [`SA.md`](./SA.md) หัวข้อ 5.3/11.2 และ schema ใน [`db/01_schema.sql`](./db/01_schema.sql)
> สถานะ: **แกนคำนวณ + สคีมา + API จำลอง + หน้าจอ W20 (ขั้นเลือก/กรอก/จำลอง) ทำเสร็จแล้ว** · เหลือ API ฝั่ง CRUD และขั้นเสนอ-อนุมัติ
> ตัวเลขทุกวิธีมีเทสต์กำกับทั้งฝั่ง TypeScript (`pnpm test`) และฝั่ง SQL (`db/04_fixed_cost_test.sql`)

## 1. ปัญหาที่มติกำลังแก้

ปัจจุบันต้นทุนคงที่ (เงินเดือนอาจารย์, ค่าเสื่อมราคา, งบสำนักงานเลขานุการ) ถูกปันส่วนด้วย
driver เดียวคือจำนวนนิสิต (`STUDENT_HEADCOUNT`) ทุกหลักสูตรเหมือนกันทั้งมหาวิทยาลัย
ผลคือ **หลักสูตรที่มีนิสิตน้อย (ป.โท/ป.เอก) รับภาระต้นทุนคงที่ต่อหัวสูงเกินจริง**
และ Q\* ของหลักสูตรเหล่านั้นสูงจนไม่มีความหมายในทางบริหาร

มติแก้ที่ **"ใครเป็นคนเลือกวิธีหาร"** — ย้ายการตัดสินใจจากกติกากลาง (กองแผนงานตั้งให้ทั้งระบบ)
มาเป็น **นโยบายรายคณะ ที่คณะเสนอเองและกองแผนงานอนุมัติ** โดยยังคงตรวจสอบย้อนหลังได้

## 2. หลักการออกแบบ 5 ข้อ

1. **วิธีหารเป็น "ข้อมูล" ไม่ใช่โค้ด** — เก็บเป็น master ที่มีเวอร์ชันรายปี + สถานะอนุมัติ
   (แนวเดียวกับ `account_behavior_rule` และ `system_setting`) เปลี่ยนวิธีแล้วคำนวณย้อนหลังได้โดยผลเดิมไม่หาย
2. **นโยบายมีผลเฉพาะกับ leg ที่เป็น `FIXED` และยังไม่ผูกหลักสูตร** — ต้นทุนคงที่ที่ ERP
   ผูกหลักสูตรได้อยู่แล้ว (direct) ไม่ต้องหาร และต้นทุนผันแปรไม่ถูกแตะ
3. **ต่อเข้าเครื่องปันส่วนเดิม ไม่สร้างเครื่องที่สอง** — ทั้ง 3 วิธีถูกแปลงเป็น `allocation_driver_value`
   ก่อนรัน กลไกหลักของ `run_cost_allocation` (largest-remainder · reconcile · quality flag · immutable run)
   จึงใช้ของเดิมทั้งหมด · สิ่งที่แก้ในตัวเครื่องมี 4 จุดและเป็นการ "ต่อสาย" ไม่ใช่เขียนตรรกะปันส่วนใหม่:
   เรียก `materialize_fixed_cost_drivers` ตอนเริ่ม · ทับวิธีปันส่วนของ leg ที่เป็น `FIXED` ด้วยนโยบายคณะ ·
   เพิ่ม `cost_pool` เข้าคีย์ค้นหา driver · ติดธง `POLICY_DEFAULTED` ให้คณะที่ยังไม่ส่งนโยบาย
4. **ยอดรวมต้องไม่เปลี่ยน** — เปลี่ยนวิธีหาร = เปลี่ยนการกระจายภายในคณะเท่านั้น
   ผลรวมต้นทุนคงที่ของคณะก่อน/หลังต้องเท่ากันเป๊ะ (เป็นเงื่อนไขผ่าน reconciliation)
5. **ต้องเห็นผลก่อนตัดสินใจ** — คณะต้องจำลองเทียบ 3 วิธีบนข้อมูลจริงของปีนั้นได้ก่อนกดเสนอ
   ไม่ใช่เลือกวิธีแบบตาบอดแล้วค่อยเห็นตัวเลขตอนสิ้นปี

## 3. ขอบเขตของการเลือก (Scope)

หนึ่งนโยบาย = **(ปีการศึกษา × คณะ × กลุ่มต้นทุนคงที่)**

| มิติ        | ค่า                                                                 | เหตุผล                                                                                                           |
| ----------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| ปี          | `academic_year` (ตาม `year_basis` เดียวกับ `account_behavior_rule`) | มติเปลี่ยนได้รายปี · ผลปีเก่าต้องไม่ขยับ                                                                         |
| คณะ         | `org_unit_id` ระดับ FACULTY                                         | มติระบุ "ให้คณะกำหนดเอง"                                                                                         |
| กลุ่มต้นทุน | `cost_pool` = `SALARY` / `DEPRECIATION` / `OFFICE_OVERHEAD` / `ALL` | ตัวอย่างในมติ (บัญชีชี้ว่าเงินเดือนกับค่าเสื่อมอาจคนละสัดส่วน) · ตั้งต้นใช้ `ALL` แถวเดียวได้ถ้าคณะไม่ต้องการแยก |

> **ค่าเริ่มต้น (default):** คณะที่ยังไม่เสนอนโยบาย → ใช้ `PER_HEAD_FTES` ซึ่งเท่ากับพฤติกรรมเดิม
> ระบบจะไม่หยุดทำงานเพราะคณะยังไม่ส่งนโยบาย แต่จะติดธง `POLICY_DEFAULTED` ให้เห็นในหน้า Exceptions

## 4. ทั้ง 3 วิธี แปลงเป็น driver อย่างไร

เครื่องปันส่วนเดิมคำนวณ `ส่วนแบ่ง = leg_amount × driver_value ÷ driver_total` — ทั้ง 3 วิธีจึงต่างกัน
แค่ "ค่าของ driver" เท่านั้น

| วิธีตามมติ                               | `allocation_method_code` | driver_value ต่อหลักสูตร                                                              | quality flag      |
| ---------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------- | ----------------- |
| 1. คิดตามรายหัวนิสิต (Per Head / FTES)   | `PER_HEAD_FTES`          | FTES ของหลักสูตรนั้นในงวด (จาก `registration_snapshot` ถ่วงน้ำหนักตาม `student_type`) | `PASS`            |
| 2. หารเท่าทุกหลักสูตรในคณะ (Per Program) | `EQUAL_PROGRAM`          | `1` ทุกหลักสูตรที่ยัง active ในงวดนั้น                                                | `PASS`            |
| 3. กำหนดสัดส่วนเปอร์เซ็นต์เอง (Custom %) | `CUSTOM_PCT`             | เปอร์เซ็นต์ที่คณะกำหนด (เก็บเป็นทศนิยม)                                               | `MANUAL_OVERRIDE` |

**เรื่อง FTES ที่ต้องฟันธงก่อนใช้จริง:** มติเขียน "Per Head / FTES" ติดกัน แต่สองอย่างนี้ไม่เท่ากัน —
headcount นับหัว ส่วน FTES ถ่วงน้ำหนักตามภาระการเรียน (เช่น ภาคพิเศษ/นอกเวลา นับไม่เต็ม 1)
เอกสารนี้เสนอใช้ **FTES** พร้อมเก็บสูตรถ่วงน้ำหนักไว้ใน master `student_type` เพื่อให้เปลี่ยนได้โดยไม่แก้โค้ด
และคง `STUDENT_HEADCOUNT` เดิมไว้เป็นอีกทางเลือกสำหรับคณะที่ต้องการนับหัวตรงๆ

### 4.1 Custom % เป็น 2 ชั้นเสมอ

ตัวอย่างในมติ ("ป.ตรี 90% · ป.โท-เอก รวมกัน 10%") ไม่ได้กำหนดถึงรายหลักสูตร แต่กำหนดถึง
**ระดับการศึกษา** แล้วในระดับนั้นยังต้องแบ่งต่ออีก โครงสร้างนโยบายจึงต้องมี 2 ชั้น:

```
ชั้นที่ 1  แบ่งก้อนต้นทุนคงที่ตามสัดส่วนที่คณะกำหนด
          bucket = ระดับการศึกษา (ป.ตรี / ป.โท / ป.เอก) หรือ รายหลักสูตรตรงๆ
          เงื่อนไข: ผลรวม % ของทุก bucket = 100.00 พอดี

ชั้นที่ 2  ภายในแต่ละ bucket แบ่งต่อลงหลักสูตรด้วยวิธีที่ 1 หรือ 2
          (sub_method = PER_HEAD_FTES | EQUAL_PROGRAM)
```

ผลลัพธ์: `driver_value ของหลักสูตร = bucket_pct × ส่วนแบ่งภายใน bucket` → ยังเป็น driver ค่าเดียวเหมือนเดิม
เครื่องปันส่วนไม่ต้องรู้ว่ามันมาจาก 2 ชั้น

ถ้าคณะกำหนด % ถึงรายหลักสูตรอยู่แล้ว ก็คือกรณีที่ bucket = หลักสูตร และชั้นที่ 2 ไม่ทำงาน

## 5. โมเดลข้อมูลที่ต้องเพิ่ม

เพิ่ม 2 ตาราง + ขยาย enum เดิม **ไม่แก้ตารางที่มีอยู่**

```sql
-- ขยาย enum ที่มีอยู่
ALTER TYPE alloc_method ADD VALUE 'PER_HEAD_FTES';
ALTER TYPE alloc_method ADD VALUE 'EQUAL_PROGRAM';
ALTER TYPE alloc_method ADD VALUE 'CUSTOM_PCT';
-- + seed allocation_method_def (reliability_rank ต่อจาก 4)

CREATE TABLE fixed_cost_policy (          -- หัวเรื่องนโยบาย 1 ฉบับ
  policy_id, org_unit_id FK(FACULTY), academic_year,
  cost_pool,                               -- ALL | SALARY | DEPRECIATION | OFFICE_OVERHEAD
  method,                                  -- PER_HEAD_FTES | EQUAL_PROGRAM | CUSTOM_PCT
  sub_method,                              -- ใช้เมื่อ method = CUSTOM_PCT
  bucket_level,                            -- EDUCATION_LEVEL | PROGRAM (เมื่อ CUSTOM_PCT)
  status,                                  -- DRAFT→SUBMITTED→APPROVED→SUPERSEDED / REJECTED
  meeting_ref, rationale,                  -- เลขที่มติ + เหตุผล (บังคับเมื่อ CUSTOM_PCT)
  submitted_by/at, approved_by/at, reject_reason,
  supersedes_policy_id FK,
  EXCLUDE: ห้ามมี APPROVED ซ้อนกันที่ (org_unit, academic_year, cost_pool)
);

CREATE TABLE fixed_cost_policy_line (     -- เฉพาะ CUSTOM_PCT
  policy_id FK, bucket_key,               -- รหัสระดับการศึกษา หรือ program_version_id
  pct numeric(7,4),                       -- 0–100
  UNIQUE (policy_id, bucket_key)
  -- CHECK ระดับตาราง: SUM(pct) = 100.0000 ต่อ policy (บังคับด้วย trigger ตอน SUBMIT)
);
```

**จุดต่อกับของเดิม:** ก่อนรัน allocation ระบบจะ **materialize** นโยบายที่ `APPROVED` ลงเป็นแถวใน
`allocation_driver_value` (period × org_unit × program_version × driver_code) พร้อม `source_reference = 'policy:<policy_id>'`
— เครื่องปันส่วนอ่านตารางเดิมตามปกติ และ audit ตามรอยกลับไปยังฉบับนโยบายได้

`account_behavior_rule.allocation_method_code` ยังใช้เหมือนเดิมกับต้นทุนผันแปรและกรณีเฉพาะ
แต่สำหรับ leg ที่ `behavior = FIXED` **นโยบายคณะมีลำดับสูงกว่า** (ลำดับการตัดสิน: direct → นโยบายคณะ → กติกาบัญชี → ค่าตั้งระบบ)

## 6. กระบวนการทำงาน 8 ขั้น

```
 [1] ตั้งปี/เปิดรอบ        กองแผนงาน      เปิด academic_year + ล็อกวันนับนิสิต (q_snapshot_date)
          │
 [2] เตรียมข้อมูลฐาน       กองแผนงาน/คณะ  นำเข้า ERP + ค่าเสื่อม + registration snapshot
          │                               → ระบบคำนวณ FTES และจำนวนหลักสูตร active ให้พร้อม
          │
 [3] จำลองเทียบ 3 วิธี     คณะ (W20)      กดดูผลทั้ง 3 แบบเคียงกันบนข้อมูลจริงปีนั้น
          │                               เห็น TFC/หัว · Q* · กำไรขาดทุน รายหลักสูตร ก่อนเลือก
          │
 [4] ร่าง + ตรวจอัตโนมัติ  คณะ            เลือกวิธี · ถ้า CUSTOM_PCT กรอก % + แนบเหตุผล/เลขที่มติ
          │                               ระบบตรวจสดตอนกรอก (ข้อ 7)
          │
 [5] เสนอ (SUBMIT)         คณบดี/ผู้มีสิทธิ์  สถานะ DRAFT → SUBMITTED · ล็อกแก้ไข
          │                               ผู้เสนอต้องคนละคนกับผู้อนุมัติ
          │
 [6] อนุมัติ / ตีกลับ      กองแผนงาน      SUBMITTED → APPROVED หรือ REJECTED (ต้องระบุเหตุผล)
          │                               APPROVED = ฉบับเก่าของปี+pool นั้นกลายเป็น SUPERSEDED
          │
 [7] รันปันส่วน            กองแผนงาน      สร้าง allocation_run ใหม่ → materialize driver จากนโยบาย
          │                               → ปันส่วน → reconcile → สรุป → คำนวณ BEP
          │                               (ขั้นตอนเดิมทั้งหมดใน SA.md 11.2 ไม่เปลี่ยน)
          │
 [8] ตรวจ + เผยแพร่        ผู้ตรวจ/กองแผน  CALCULATED → VALIDATED → APPROVED → ขึ้น dashboard
                                          พร้อมป้ายกำกับว่าตัวเลขชุดนี้ใช้นโยบายฉบับไหน
```

### ใครทำอะไร (RACI ย่อ)

| ขั้น                 | คณะ (`faculty_officer`) | กองแผนงาน (`budget_office`) | ผู้บริหาร/`viewer` |
| -------------------- | ----------------------- | --------------------------- | ------------------ |
| 1–2 เตรียมข้อมูล     | ร่วมนำส่ง               | รับผิดชอบ                   | —                  |
| 3–5 เลือก/เสนอนโยบาย | **รับผิดชอบ**           | ให้คำปรึกษา                 | รับทราบ            |
| 6 อนุมัติ            | รับทราบ                 | **รับผิดชอบ**               | —                  |
| 7–8 คำนวณ/เผยแพร่    | รับทราบ                 | **รับผิดชอบ**               | ใช้ผล              |

## 7. กติกาตรวจสอบ (บังคับที่ระดับ service ไม่ใช่แค่ UI)

| #   | กติกา                                                         | ผลเมื่อไม่ผ่าน                                                 |
| --- | ------------------------------------------------------------- | -------------------------------------------------------------- |
| V1  | `SUM(pct) = 100.0000` พอดี (ไม่มี tolerance)                  | กดเสนอไม่ได้ · แสดงส่วนต่างสดตอนกรอก                           |
| V2  | ทุกหลักสูตร active ในปีนั้นต้องอยู่ใน bucket ใด bucket หนึ่ง  | กดเสนอไม่ได้ · ชี้ชื่อหลักสูตรที่ตกหล่น                        |
| V3  | bucket ที่ไม่มีหลักสูตรอยู่เลย แต่ได้ % > 0                   | กดเสนอไม่ได้ (ไม่งั้นเงินไปค้างไม่มีเจ้าภาพ)                   |
| V4  | `CUSTOM_PCT` ต้องมี `rationale` + `meeting_ref`               | กดเสนอไม่ได้                                                   |
| V5  | ผู้อนุมัติ ≠ ผู้เสนอ                                          | อนุมัติไม่ได้                                                  |
| V6  | นโยบายต้อง `APPROVED` ก่อน จึงรัน allocation ของปีนั้นได้     | คณะไหนยังไม่อนุมัติ → ใช้ default + ติดธง `POLICY_DEFAULTED`   |
| V7  | หลัง materialize: ผลรวมต้นทุนคงที่ที่ปันได้ = ยอด pool ต้นทาง | run `FAILED` ตาม reconciliation เดิม                           |
| V8  | `sub_method = PER_HEAD_FTES` แต่ bucket นั้น FTES รวม = 0     | ถอยไปใช้ `EQUAL_PROGRAM` ภายใน bucket + ติดธง `MISSING_DRIVER` |

## 8. หน้าจอ

**ใหม่ — W20 นโยบายต้นทุนคงที่รายคณะ**

- หัวหน้าจอ: ตัวกรองกลางชุดเดิม (ปีการศึกษา · คณะ · กลุ่มต้นทุน) + ป้ายสถานะนโยบาย
- **Stepper 4 ขั้น**: เลือกวิธี → กำหนดสัดส่วน → ตรวจผลจำลอง → เสนออนุมัติ
- ขั้น "เลือกวิธี": การ์ด 3 ใบ อธิบายผล/ข้อดี-ข้อเสียของแต่ละแบบเป็นภาษาคน
- ขั้น "กำหนดสัดส่วน" (เฉพาะ Custom %): ตารางแก้ไขในตัว + แถบรวม % ที่เปลี่ยนสีทันทีเมื่อ ≠ 100
  · ปุ่ม "เฉลี่ยส่วนที่เหลือ" · บันทึกร่างอัตโนมัติกันปิดหน้าจอแล้วหาย + ปุ่มล้างร่าง
- ขั้น "ตรวจผลจำลอง": ตาราง/กราฟเทียบ **วิธีปัจจุบัน vs วิธีที่เลือก** รายหลักสูตร
  (TFC, ต้นทุนคงที่/หัว, Q\*, กำไรขาดทุน) · ตัวเลขสรุปคลิกเจาะลงรายหลักสูตรได้
- ขั้น "เสนอ": ฟอร์มเหตุผล + เลขที่มติ → ยืนยันด้วย dialog (การเสนอล็อกการแก้ไข)
- แท็บ "ประวัติ": ทุกฉบับย้อนหลัง ใครเสนอ/อนุมัติ/เมื่อไหร่ · เทียบฉบับต่อฉบับได้

**หน้าที่ได้รับผลกระทบ**

| หน้า               | สิ่งที่ต้องเพิ่ม                                                              |
| ------------------ | ----------------------------------------------------------------------------- |
| W11 Allocation Run | แสดงว่า run นี้ใช้นโยบายฉบับไหนของแต่ละคณะ · เตือนถ้ามีคณะที่ใช้ default      |
| W12 Reconciliation | แยกยอดคงที่/ผันแปร ให้เห็นว่านโยบายไม่ทำยอดรวมเพี้ยน                          |
| W13 Exceptions     | คิวใหม่: `POLICY_DEFAULTED`, `POLICY_PENDING`, bucket ที่ไม่มีหลักสูตร        |
| W14 Account Rules  | หมายเหตุว่า leg `FIXED` ถูกนโยบายคณะทับ (กันความสับสนว่าทำไมตั้งไว้แล้วไม่ผล) |
| W2/W4b รายงาน      | ป้ายกำกับใต้ตัวเลข: "ปันส่วนต้นทุนคงที่ด้วยวิธี X ตามมติ …"                   |

## 9. ผลกระทบต่อการคำนวณ

- กระทบเฉพาะ **TFC รายหลักสูตร** → เปลี่ยน `Q*`, `ATC`, `π` รายหลักสูตร
- **ไม่กระทบ** `AVC`, `R`, TVC และ **ไม่กระทบยอดรวมระดับคณะ/มหาวิทยาลัย** ในโหมด `pooled`
- แต่ **กระทบ Q\* ระดับคณะในโหมด `sum_of_programs`** (ค่าหลักตามค่าตั้งปัจจุบัน) เพราะผลรวมของ
  Q\* รายหลักสูตรไม่เป็นเชิงเส้นเมื่อบางหลักสูตรตกเข้ากรณี CM ≤ 0 — **ต้องสื่อสารข้อนี้กับผู้บริหาร**
- `packages/calc-engine` **ไม่ต้องแก้สูตร** — การเปลี่ยนแปลงทั้งหมดอยู่ที่ input (TFC) ไม่ใช่สูตร
  ส่วนที่ต้องเพิ่มคือฟังก์ชัน pure ตัวใหม่สำหรับ "แปลงนโยบาย → driver" พร้อม test ของมันเอง
- Golden test เดิมต้องยังผ่าน โดยกำหนดให้ทุกคณะในชุดทดสอบใช้ `PER_HEAD_FTES` = พฤติกรรมเดิม

## 10. ธรรมาภิบาลและการตรวจสอบย้อนหลัง

1. นโยบาย **แก้ไม่ได้หลังอนุมัติ** — ต้องออกฉบับใหม่ที่ชี้ `supersedes_policy_id`
2. เปลี่ยนนโยบายกลางปี → ไม่ลบผลเดิม แต่สร้าง `allocation_run` ใหม่ที่ชี้ `supersedes_run_id`
   ผู้บริหารย้อนดูได้ว่าตัวเลขที่เคยเห็นเมื่อเดือนก่อนมาจากนโยบายฉบับใด
3. ทุกการเปลี่ยนสถานะลง `audit_event` (ใคร / เมื่อไหร่ / ค่าเดิม→ค่าใหม่)
4. รายงานทุกใบระบุที่มา: ปี + วันที่คำนวณ + `revenue_mode` + **ฉบับนโยบายต้นทุนคงที่**

## 11. กรณีขอบที่ต้องรองรับ

| กรณี                                      | การจัดการ                                                                                                                                   |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| หลักสูตรเปิดใหม่/ปิดกลางปี                | ใช้ `program_version` ที่ active ณ `q_snapshot_date` เท่านั้น · หลักสูตรใหม่ที่เกิดหลังอนุมัตินโยบาย → เข้าคิว Exceptions ให้คณะแก้ฉบับใหม่ |
| หลักสูตรนิสิต 0 คน                        | `PER_HEAD_FTES` ให้ส่วนแบ่ง 0 (ไม่ใช่ error) · `EQUAL_PROGRAM` ยังได้ส่วนแบ่งเต็ม — ต้องเตือนคณะตอนจำลองว่าสองวิธีให้ผลต่างกันมากในกรณีนี้  |
| คณะไม่มีหลักสูตรเลย (หน่วยสนับสนุน)       | ไม่มีนโยบาย · ยังถูกปันออกด้วยกลไก `is_academic = false` เดิม                                                                               |
| ค่าเสื่อมราคาไม่มีรหัสผังบัญชี            | ใช้ `depreciation_behavior` เดิมกำหนดว่าเป็น FIXED แล้วเข้า pool `DEPRECIATION`                                                             |
| ต้นทุนคงที่ติดลบ (รายการปรับปรุง/คืนเงิน) | ปันส่วนตามสัดส่วนเดียวกัน ค่าที่ได้ติดลบได้ · reconcile ตามปกติ                                                                             |
| มติเปลี่ยนย้อนหลังข้ามปี                  | อนุญาต แต่บังคับให้ระบุเหตุผล + สร้าง run ใหม่ ไม่ทับของเดิม                                                                                |

## 12. ลำดับการลงมือ

- [x] **สคีมา** — ขยาย `alloc_method` (+3 ค่า) · enum `fixed_cost_pool` / `bucket_level` ·
      `student_type.ftes_weight` · `allocation_driver_value.cost_pool` ·
      ตารางใหม่ `fixed_cost_policy` / `fixed_cost_policy_line` / `fixed_cost_pool_rule`
      → [`db/01_schema.sql`](./db/01_schema.sql)
- [x] **calc-engine** — `buildFixedCostDrivers()` · `allocateAmount()` (largest remainder รองรับยอดติดลบ) ·
      `allocateFixedCost()` · `simulateFixedCostMethods()` เทียบ 3 วิธีพร้อม Q\* รายหลักสูตร
      → [`packages/calc-engine/src/fixed-cost-policy.ts`](./packages/calc-engine/src/fixed-cost-policy.ts) ·
      [`fixed-cost-simulation.ts`](./packages/calc-engine/src/fixed-cost-simulation.ts) · **33 เทสต์**
- [x] **เครื่องปันส่วน (SQL)** — `program_ftes()` · `fixed_cost_driver_values()` ·
      `fixed_cost_policy_issues()` (V1–V4) · `materialize_fixed_cost_drivers()` ·
      `run_cost_allocation()` เรียก materialize เอง + ลำดับกติกา + ธง `POLICY_DEFAULTED`
      → [`db/02_functions.sql`](./db/02_functions.sql)
- [x] **API จำลอง** — `POST /api/fixed-cost/simulate` (คำนวณล้วน ไม่เขียน DB · ต้องมี session)
      → [`apps/web/src/app/api/fixed-cost/simulate/route.ts`](./apps/web/src/app/api/fixed-cost/simulate/route.ts) ·
      schema ร่วม frontend/backend ที่ [`packages/shared-types/src/fixed-cost.ts`](./packages/shared-types/src/fixed-cost.ts)
- [x] **เทสต์** — golden test เดิมผ่านครบ (ไม่มี regression) · เทสต์ใหม่ยืนยันว่าเปลี่ยนวิธีแล้ว
      ยอดรวมคณะไม่เปลี่ยนและ reconcile ยังตรงพอดี → [`db/04_fixed_cost_test.sql`](./db/04_fixed_cost_test.sql)
- [ ] ยืนยันข้อค้างในหัวข้อ 13 กับที่ประชุม (ข้อ 1 กับ 2 มีทางเลือกเตรียมไว้ในโค้ดแล้ว)
- [ ] API ฝั่ง CRUD นโยบาย + สถานะ DRAFT→SUBMITTED→APPROVED (ยังไม่มี backend service layer ในโปรเจกต์)
      · ตอนต่อ API ต้องบังคับ V4 (เหตุผล + เลขที่มติ) เป็นด่านจริง — ตอนนี้หน้าจอแสดงเป็น inline error
      เฉยๆ เพราะปุ่ม "เสนอ" ยังปิดอยู่
- [x] **หน้าจอ W20** — ตัวกรอง (ปี × คณะ × กลุ่มต้นทุน) · Stepper 4 ขั้น · การ์ด 3 วิธี ·
      ตารางกรอก % + แถบรวมสด + ปุ่มเฉลี่ยส่วนที่เหลือ + ร่างอัตโนมัติ · ตารางเทียบรายหลักสูตร
      ที่เจาะลงรายหลักสูตรได้ → [`apps/web/src/views/fixed-cost-policy/`](./apps/web/src/views/fixed-cost-policy/)
      · ขั้น "เสนอ" ยังกดไม่ได้ (รอ API ฝั่ง CRUD) และข้อมูลที่ป้อนเข้าจำลองยังมาจากชุดตัวอย่าง `RAW`
- [ ] ปรับ W11/W12/W13/W14 + ป้ายกำกับในรายงาน
- [ ] คู่มือคณะ 1 หน้า: เลือกวิธีไหนเมื่อไหร่ + ตัวอย่างกรอก %

> **ข้อค้าง 2 ข้อที่โค้ดเตรียมทางไว้แล้ว ไม่ต้องรอมติจึงจะเดินต่อได้**
> ข้อ 1 (FTES หรือ headcount): ตั้ง `student_type.ftes_weight = 1` ทุกประเภท → FTES กลายเป็นการนับหัว
> ข้อ 2 (แยกกลุ่มต้นทุน): ใช้ฉบับ `cost_pool = 'ALL'` ฉบับเดียวก็พอ · จะแยกทีหลังค่อยเพิ่มฉบับเจาะจงกลุ่ม

## 12.1 ข้อจำกัดที่รู้อยู่ (ตรวจพบจากการ review — ยังไม่แก้โดยตั้งใจ)

| #   | เรื่อง                                                                                                                                                                                                                                                                                            | ผลกระทบ / ทางแก้เมื่อถึงเวลา                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | หน้าจำลองปันทั้งก้อนครั้งเดียว แต่ run จริงปัน **ทีละรายการต้นทุน** (leg)                                                                                                                                                                                                                         | ตัวเลขต่างกันได้ระดับสตางค์ต่อหลักสูตร (ไม่กระทบยอดรวมและการตัดสินใจเชิงนโยบาย) · ถ้าต้องตรงเป๊ะ ต้องส่งรายการต้นทุนทั้งชุดเข้าหน้าจำลองแทนยอดก้อนเดียว |
| 2   | นโยบายที่ **ทุกกลุ่มไม่มีหลักสูตรเลย** ให้ผลต่างกัน: หน้าจำลองคืนส่วนแบ่ง 0 ทุกหลักสูตร · run จริงเก็บยอดไว้เป็น `MISSING_DRIVER`                                                                                                                                                                 | ทั้งสองทางไม่ทำให้ยอดหาย และนโยบายแบบนี้ถูกกันไม่ให้อนุมัติอยู่แล้ว (trigger `fixed_cost_policy_guard`)                                                 |
| 3   | รหัสข้อทักท้วงสองฝั่งไม่เท่ากัน — `BUCKET_DUPLICATED` / `PCT_INVALID` ฝั่ง SQL ฝากไว้กับ constraint และ `FTES_UNAVAILABLE` ระดับกลุ่มมีเฉพาะฝั่ง TS                                                                                                                                               | ข้อความบนหน้าจอกับที่ API ตอบอาจไม่เหมือนกันในเคสขอบ · ทั้งคู่ยังบล็อกการอนุมัติเหมือนกัน                                                               |
| 4   | ยังไม่มีเทสต์อัตโนมัติที่เทียบตัวเลข TypeScript กับ PostgreSQL ตรงๆ                                                                                                                                                                                                                               | ตอนนี้ตรวจด้วยมือแล้วว่ากติกาปัดเศษตรงกัน (รวมยอดติดลบ) · ควรทำ harness ที่ดึงค่าจาก DB จริงมาเทียบใน CI                                                |
| 5   | หน้าจอ W20 ประกอบ input จากชุดข้อมูลตัวอย่าง `RAW` (ยังไม่มี API อ่านข้อมูลจริงรายคณะ) · ใช้จำนวนนิสิตเป็น FTES (น้ำหนักทุกประเภท = 1) · ตัวเลือกกลุ่มต้นทุนบนจอมีแค่ `ALL`/`OFFICE_OVERHEAD`/`DEPRECIATION` และ `ALL` ที่นี่ = สำนักงาน + ค่าเสื่อม (ไม่รวมเงินเดือน เพราะ `RAW` ยังไม่แยกมาให้) | ตัวเลขที่เห็นเป็นรูปทรงที่ถูกต้องแต่ยังไม่ใช่ของปีจริง · เปลี่ยนเฉพาะที่ `views/fixed-cost-policy/data.ts` เมื่อ API ขั้นที่ 2 พร้อม                    |

## 13. สิ่งที่ยังต้องขอมติเพิ่ม

1. **FTES หรือ headcount** — มติเขียนติดกัน แต่ให้ผลต่างกันจริงในหลักสูตรภาคพิเศษ (หัวข้อ 4)
2. **แยกกลุ่มต้นทุนได้หรือไม่** — คณะเลือกวิธีเดียวคุมต้นทุนคงที่ทั้งก้อน หรือแยกเงินเดือน/ค่าเสื่อม/ค่าสำนักงานได้คนละวิธี
3. **เพดานของ Custom %** — ต้องมีกรอบกันการตั้งสัดส่วนที่ทำให้หลักสูตรหนึ่งแทบไม่รับภาระเลยหรือไม่
   (เช่น บังคับว่า ต้นทุนคงที่/หัว ของ bucket ใดต้องไม่ต่ำกว่า X% ของค่าเฉลี่ยคณะ)
4. **ระดับที่อนุมัติ** — กองแผนงานอนุมัติพอ หรือต้องผ่านคณะกรรมการ/มติสภาฯ ก่อน
5. **ปีแรกที่บังคับใช้** — 2568 ย้อนหลัง หรือเริ่ม 2569 เป็นต้นไป
